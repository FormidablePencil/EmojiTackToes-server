import express from 'express'
import mongoose from 'mongoose'
const retrieveUserData = express.Router()

retrieveUserData.get('/userdata', async (req, res, next) => {
  
})

export default retrieveUserData